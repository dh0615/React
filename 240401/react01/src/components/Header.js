const Header = () => {
  return (
    <header>
      <h1>header</h1>
    </header>
  );
};

export default Header;
// 다른 곳에서도 사용할 수 있다고 선언해줌 선언 후 App.js 가서 import Header from "./components/Header" 작성해줘야함
